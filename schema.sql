CREATE TABLE IF NOT EXISTS github_users (
                                            login TEXT PRIMARY KEY,
                                            name TEXT,
                                            created_at TEXT
);